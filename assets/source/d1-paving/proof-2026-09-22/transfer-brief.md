# Current restored-family transfer

The relighting diagnostic and two independent screens support retaining the restored
material; human soil-to-stone edge approval remains in force. Next dependency is
fresh portable evidence for this exact family, not the older normalized material.

One build/bake/import package, 25 active / 40 elapsed minutes, up to one technical
repair if export validation fails. Use Blender 5.2.1 and the existing study bake and
verification procedure adapted into new output folders. Three representative stones
at <=4000 triangles each, current base color/normal/roughness sizes per existing
stone QA class. Preserve high sources and existing soil untouched. Use a neutral
temporary studio with only the target stone, camera, light and neutral ground; do
not accidentally bake the dense soil/grit scene into a stone export.

Inspect actual file structure, geometry/UV basic validity and a fresh import against
the matching native low asset. Two independent visual screens and lead inspect
current reimports before dependent replication. Include an opposing-light check on
the fresh imports. Record hashes, source lineage, bytes, triangles, bake/import
timings and what validations did and did not run.

These are early source-only transfer artifacts. No budget relaxation, library
admission or public upload: existing 8 MiB resource, shared-material, compression,
LOD and full QA requirements remain. Preserve the accepted contact arrangement;
this neutral-studio transfer is not another contact appearance test.

Outcome: one three-member run completed in 116.17 seconds. Focused checks passed;
both independent visual screens conditionally support the 4 m study. Preserve the
current exports as source evidence. All raw files exceed 8 MiB and a localized C
shading mark requires final delivery correction. See [results](transfer-results.md).
