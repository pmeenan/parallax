# Sidewall1 author screen

All five matched native views and the gray joint diagnostic inspected against the
retained family-contact2 and previous profile-contact2 gray diagnostic. Local sidewall
facets are now visible in gray, particularly along A's exposed joint edge and C's
front edge. Top interiors, mineral material identities and soil remain unchanged.

The main long dark band remains in textured joint-detail. The edit interrupts portions
of the smooth profile but does not visibly resolve the dominant strip, so this is
not an acceptance claim or authority to enlarge the paving pattern. Independent
quality/theme screens and lead adjudication should decide whether to retain the
localized wear or reject it. No second candidate was authored.

Construction and six captures took26.09seconds on pinned Blender5.2.1. All nonstone
coordinate hashes matched the baseline. The inner top regions are byte-identical;
only coordinates within22mm of perimeter and above60.5mm worldheight could move.
The retained feature receipt specifies every facet center, span and depth. The
previous external height input is packed in the editable source.

Focused validation found finite coordinates on all three stones, unchanged vertex
and face counts, zero degenerates and zero normal flips among20000 deterministic
edited triangles sampled per stone. Lowest sampled normal dot product is0.8606.
This is not a full self-intersection, topology, asset-QA or runtime test.

Reproduce `build.py` in a fresh background Blender5.2.1 instance with factory-startup
and python-exit-code1. Run `verify.py` separately for the focused read-only comparison.
Original family/contact files were read only; all outputs are in this new directory.
