node_modules/
dist/
*/dist/
*.tsbuildinfo
engine/wasm/*/pkg/
engine/wasm/*/target/
harness/results/
# Harness evidence is machine-local even if a runner-relative cwd bug adds a directory level.
harness/**/results/
harness/chrome/cft/
*.patch
.claude/

# Machine-local absolute paths for pinned tools (see docs/workflow.md).
.parallax-toolchain.local.json

# Immutable binary library cache; checked-in manifests pin every admitted object (D-186).
assets/library/objects/
